#include <iostream>
#include <map>
#include <string>

#include "edge.h"
#include "UndirectedGraph.h"
#include "GraphProcessor.h"

using namespace std;
using namespace GraphProcessor;

template<class T>
string LoadGraph(string filename, UndirectedGraph<T> newGraphInstance)
{
	// string line;
	// ifstream myfile ("example.txt");
	// if (myfile.is_open())
	// {
	// 	while ( getline (myfile,line) )
	// 	{
	// 		std::cout << line << '\n';
	// 	}
	// 	myfile.close();
	// }
	// 
	// else cout << "Unable to open file"; 
	// 
	return "0";
}
